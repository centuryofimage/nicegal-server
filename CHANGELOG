- fix library migrations in edge cases
- clearer errors: search warnings open a dialog with the fix, search setup notices are no longer shown as errors, and fewer errors cover the whole gallery
- speed up deletions
- video indexing is now a per-library option in Library manager
- changing the image model or execution provider waits until Settings closes before rescanning
